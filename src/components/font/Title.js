import React from 'react';

const Title = () => {
    return (
        <div>
            <h1>How React Works?</h1>
            <h4>React creates hassle-free interactive UI and design a simple view for each state of  application, and React will efficiently update and render the correct component whenever the data changes
                Declarative view makes  code more straightforward and easily debugable.</h4>

            <br />

            <h1>How React useState Work?</h1>
            <h4>useState is a function that allows  to have state variables in functional components. This pass the initial state to this function and it returns a variable with the current state value  and another function to update this value</h4>
        </div>
    );
};

export default Title;